let i = 0;
const values = [];

function getGoldAmount(){
    let total = 0;
    let average = 0;

    values[i] = parseInt(document.getElementById('amount').value);
    i++;
    document.getElementById('amount').value = ""
    
    values.forEach(val => {
        total = total + val;
    });
    average = total / i;
    document.getElementById('average').innerHTML = average;
}